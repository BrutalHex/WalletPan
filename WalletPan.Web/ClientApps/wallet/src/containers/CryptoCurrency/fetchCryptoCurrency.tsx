
/*
import {postData,getData} from '../../base/fetchService';
import {getApiUrl} from '../../base/settings';

import {fetchCryptoCurrencyPending, fetchCryptoCurrencySuccess, fetchCryptoCurrencyError} from './action';



  function fetchCryptoCurrency() {
      debugger;
      let link=getApiUrl('CryptoCurrency/GetAll');

    return   dispatch =>  {
      
        dispatch(fetchCryptoCurrencyPending());

         getData(link,(data)=> {
 
         dispatch(fetchCryptoCurrencySuccess(data));
        
        });
    

         
     
    }
}

export default fetchCryptoCurrency;*/
